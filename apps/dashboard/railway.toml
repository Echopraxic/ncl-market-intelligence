[build]
builder = "dockerfile"
dockerfilePath = "apps/dashboard/Dockerfile"

[deploy]
startCommand = "node apps/dashboard/server.js"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
